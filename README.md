# RC-Car-speedometer
This code works on a PIC microcontroller. The Following parts will be listed soon and so will the code. Stay tunned!
Will need to attach oled.c and oled.h to project and any other non main files for things to work.
Also remove the timeout.h and any files needed to timeout.
Each time we regenerate in system it brings back a reference to timeout.h will need to remove. 
HALL Effect code now has display functionality and live readings as well.
